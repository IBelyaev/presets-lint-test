name: Create version

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: create new version
        run: echo 'Create new version onl y for master push ' &&  ls -a #&& yarn --frozen-lockfile && yarn --cwd ${{ github.workspace }} semantic-release